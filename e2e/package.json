{
  "name": "src",
  "version": "1.0.0",
  "description": "Dummy project to install Cypress plugins",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "devDependencies": {
    "cypress-file-upload": "^5.0.8"
  }
}
